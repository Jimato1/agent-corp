"""library.corpus — the CANONICAL store: markdown + YAML frontmatter, git-backed.

Single-writer service core (PLAN §1.1). index.db is a rebuildable projection of
THIS; nothing here is derived from the index.
"""
