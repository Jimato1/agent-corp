"""library.index — the REBUILDABLE projection (index.db): chunker + FTS5 + vectors.

Pure function of the canonical corpus (PLAN §1.3). Destroy-and-rebuild is a
first-class operation proven at Stage 7. No column here holds data that does not
originate in corpus frontmatter/bodies.
"""
