# RATIFICATIONS — 2026-07-02 (operator rulings on MERGE_REVIEW_1 §4, encoded)

> **Status:** AUTHORITATIVE decision ledger. Each row records the operator's ruling on a MERGE_REVIEW_1 decision and where it is now encoded. Docs-only session on branch `merge/research-merge-1`; no app planned, no src touched, no spike/sizing run. Rows tagged **[posture]** are deliberate operator-posture choices, revisitable by a future ratification (never by silent drift).

| # | Decision | Ruling | Encoded in |
|---|---|---|---|
| D-1 | Board huddle tie-break authority | **RATIFIED AS WRITTEN** — DACI split: SM process-only (server-side Board watchdog enforces timebox/round-cap), PO sole Recommender-of-record, AR scoped veto + mechanically-forced grounded dissent (≥1 recon-cited premise-attack or huddle invalid), reversibility always derived, auto-escalation on round-cap/timebox/unresolved-veto, huddle never the last signature; defaults 3 rounds / 2–4 role agents | `TICKET_STATE_MACHINE.md` §3 "Ceremony governance"; `apps/board/CLAUDE.md` SETTLED #2; MERGE_REVIEW_1 §6 Board register |
| D-2 | Ceremony scaling | **RATIFIED AS WRITTEN** — deterministic 3-lane triage from five signals; straight-to-execute requires all four hard conditions; children inherit lane as floor; missing/stale signal → full ceremony; lane governs planning rigor only; never an LLM score | `TICKET_STATE_MACHINE.md` §3 "Ceremony governance"; `apps/board/CLAUDE.md` SETTLED #3; §6 register |
| D-3 | `mc` vs `mission-control` naming | **RATIFIED** — MC's service/subdomain/audience all become **`mc`**; built auth's `mc` audience stands; directory stays `apps/mission-control/` | `DEPLOYMENT.md` §2 (new `mc` row); `apps/mission-control/CLAUDE.md` SETTLED #1; `CONTRACTS/auth-apps-tokens-scopes.md` §5 |
| D-4 | Vault redemption precondition placement | **RATIFIED** — Gateway-side enforcement baseline PLUS Vault's redeem endpoint independently verifies the approval, **naming `approval_id`** (Board API call at release or Board-signed offline evidence; Vault Stage-2 picks the mechanism) | `CONTRACTS/vault-gateway-redemption.md` §3 (now binding); `apps/vault/CLAUDE.md` SETTLED #2 |
| D-5 | State-machine amendments | **RATIFIED BOTH** — **A1 `board_escalation`**: Board-automatic `in_progress → needs_review` (stuck-huddle watchdog / max-renewal force-escalate); **A2 `breakglass_review_ticket`**: post-break-glass review ticket born in `needs_review` (the only non-`todo` birth state). Both narrow; neither opens an agent/policy path toward execution | `TICKET_STATE_MACHINE.md` §2 (two new named rows + guard note) |
| D-6 | CMDB host-facts duties | **RATIFIED** — CMDB accepts snapshot capability, per-tier health/SSH timeouts, Syscollector scan-interval, mid-run window-close semantics; fail-closed escalations land as **Board escalations** | `CONTRACTS/cmdb-gateway-policy.md` §6 (now duties); `apps/cmdb/CLAUDE.md` SETTLED #2; §6 register |
| D-7 | Sandbox `disposable` class | **RATIFIED** — mandatory **Stage-2 EXIT item for BOTH Gateway and CMDB**, with an explicit non-leakable carve-out from the destructive-never-auto floor; Library curation go-live blocked on it | `CONTRACTS/gateway-cmdb-library-sandbox.md` (ruling banner); `apps/gateway/CLAUDE.md` SETTLED #3; `apps/cmdb/CLAUDE.md` SETTLED #3; §6 register |
| D-8 | Gateway private Postgres | **RATIFIED [posture — first invariant exception]** — deliberate, named exception to DEPLOYMENT §3 SQLite-per-app (`data_gateway` network), justified by the signed append-only audit chain + advisory-lock mutex; recorded as "invariant exception #1" so it reads as a ruling, not drift | `DEPLOYMENT.md` §3 exception note; `apps/gateway/CLAUDE.md` SETTLED #2 |
| D-9 | Wazuh webhook auth + closure path | **RATIFIED** — static HMAC shared secret (raw-body-signed) now; revisit at auth machine-principal enrollment. CF-H confirmed: closure = document **disappearance** via connector poll; Solved events advisory only | `CONTRACTS/board-wazuh-connector-kickoff.md` §5 |
| D-10 | Deployment amendments batch | **RATIFIED IN PRINCIPLE** — named sidecar convention (`<owner>_<function>`, owner-scoped networks, no host ports, one DEPLOYMENT row per sidecar at the owning app's Stage-2). Owners: ntfy→chat; Prometheus+blackbox→MC; **log shipper + store → MC (ownership assigned — was unowned)**; unsealer + off-box WORM sink→vault | `DEPLOYMENT.md` new §3a (convention + owner table); MC/chat/vault CLAUDE.md SETTLED entries |
| D-11 | Loop-guard / spawn-depth owner | **RATIFIED** — Board enforces lineage/spawn-depth caps at claim time; MC surfaces + auto-triages; auth keeps token/identity budgets only | `apps/board/CLAUDE.md` SETTLED #4; `apps/mission-control/CLAUDE.md` SETTLED #4; §6 register |
| D-12 | Correlated-loss suppression thresholds | **DEFERRED** — numbers await the gap-1.2 sizing measurement; the Board reaper adopts the same population gate once set. No value encoded anywhere by design | pending marker only (this ledger + §6 register); contracts already carry the mechanism sans numbers |
| D-13 | Library embeds via runtime TEI | **RATIFIED** — one guarded provenance surface; agent-runtime is a hard dependency of Library indexing | `CONTRACTS/agent-runtime-library-inference.md` §4; `platform/agent-runtime/CLAUDE.md` SETTLED #2 |
| D-14 | Adopt-vs-build batch | **RATIFIED ALL** — Vault=OpenBao 2.5.x (≥2.5.5, BUSL fallback break-glass only) · CMDB=bespoke thin fail-closed registry · Chat=bespoke thin + ntfy sink only · Drive=filesystem-CAS + SQLite (Versity = S3 swap-path) · Gateway=embedded ansible-runner playbook-only · agent-runtime=vLLM/SGLang/llama.cpp/TEI + Sigstore gate · Board=SQLite CAS + node-cron + **MCP pinned 2025-11-25 suite-wide** | SETTLED DECISIONS sections in `apps/{vault,cmdb,chat,drive,gateway,board}/CLAUDE.md` + `platform/agent-runtime/CLAUDE.md` |
| D-15 | `svc:tier-approver` runner | **RATIFIED** — Board hosts it as an internal service process (approval state is Board's; queries the CMDB verdict; auth kind-gates it from destructive/high tiers) | `CONTRACTS/auth-apps-tokens-scopes.md` §6; `apps/board/CLAUDE.md` SETTLED #5; §6 register |
| D-16 | Vault operational trio | **RATIFIED AS RECOMMENDED [posture ×3 — each tagged "operator-posture, revisit before prod"]** — (a) fail-closed-on-audit: redemption refused if unloggable (availability traded for non-repudiation); (b) WORM sink = **hardened log host**; (c) seal = on-prem Transit auto-unseal via second minimal unsealer + 3-of-5 offline recovery shares | `apps/vault/CLAUDE.md` SETTLED #3 (with posture tags); `DEPLOYMENT.md` §3a (unsealer/WORM rows) |
| D-17 | Spike gate hardness + thresholds | **RATIFIED SOFT-START / HARD-FREEZE** — drafting may proceed; NO tool-schema freeze and NO Stage-2 exit for Board/Notes until the gap-1.3 spike PASSES. Thresholds ratified as proposed (N≥50/model; zero-tolerance SoD hard gate; 0 transition-schema violations; ≥95% clean success; 100% termination ≤2× optimal; ≥95% graceful escalation / 0% spin). All MCP apps inherit a schema-complexity ceiling | `PROCESS.md` Stage-2 entry gate; `platform/agent-runtime/CLAUDE.md` SETTLED #3 |
| MC-btn | Kill-switch sub-question | **RATIFIED [posture]** — MC **keeps a kill button**, wired to CALL auth's revocation; the button is the cockpit affordance, never a second enforcer; auth stays the single enforcement point; fail-loud + hand-off on failure | `CONTRACTS/killswitch-chain.md` §1; `apps/mission-control/CLAUDE.md` SETTLED #2 |

## Defect fix ratified alongside

**`/_context/` import-path bug — FIXED suite-wide (11 files):** the per-app "Read `/_context/ARCHITECTURE.md`…" pointer was broken in `platform/auth`, `platform/proxy`, `apps/{board, notes, mission-control, drive, chat, gateway, vault, cmdb}` CLAUDE.md, plus one prose reference in root CLAUDE.md — all corrected to the working `context/` form. Mitigating fact: the root CLAUDE.md `@context/…` auto-imports meant ARCHITECTURE/PROCESS still loaded in every session regardless. **FLAG:** Vault's and CMDB's Stage-1 research may nonetheless have run without the tier-1 specs deliberately loaded — **both carry a Stage-2 obligation to re-check their RESEARCH.md against the frozen contracts + tier-1 specs before planning hardens** (recorded in MERGE_REVIEW_1 §6 and their CLAUDE.md files).

## Confirmed next gate

**Before ANY Stage-2 planning:** run, as their own dedicated sessions, (1) the **gap-1.3 local-model-drives-MCP feasibility spike** (gates Board/Notes per D-17), then/alongside (2) the **gap-1.2 model-serving sizing measurement** (needs the six operator inputs; unblocks ceremony parameters, D-12 thresholds, and the Library index schema).
