"""auth.core — the frozen, dependency-free contracts every builder imports.

Pure stdlib. No I/O, no crypto primitives, no store here — only the taxonomy,
the immutable SoD invariant, the data model, the Protocol interfaces, and the
typed errors. These are the contracts the parallel Stage-4 builders depend on.
"""
